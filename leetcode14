python切片语法

sequence[start:stop:step]
其中：

start 是切片开始的位置。
stop 是切片结束的位置（不包括这个位置的元素）。
step 是每次跳过的元素数目，默认为 1。
在 [:i] 这个切片操作中：从0开始到i,不包括i

start 默认为 0，即从序列的开始位置。
stop 设置为 i。
step 默认为 1，即每个元素都被选取，直到但不包括索引 i。

s = "Hello"
s[:3] 的结果是 "Hel"。这是因为它取从索引 0 到索引 3（不包括 3）的字符。
s[:1] 的结果是 "H"。取索引 0 的字符，不包括索引 1。
s[:0] 的结果是 ""。没有包括任何字符，因为它结束于索引 0，不包括这个索引。




思路:纵向扫描,比较string同一位置的字符,看是不是一样,不是就return出去
可以先initial最小长度为这个list中最短的string长度(找共同的prefix,看最短的字符串
